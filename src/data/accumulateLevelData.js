const accumulateLevelData = (character, nLevel) => {
    const base = {
        ...((character.levels||[])[0] || {})
    }
    for(let i=1; i<nLevel; i++){
        
    }
    return base
}

export default accumulateLevelData